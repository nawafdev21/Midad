# مواصفات بناء موقع مقالات ونشرة عربية (RTL)

> مرجع مستوحى من بنية موقع «ورشة مخ». المكدّس المستهدف: **Astro (SSG) + beehiiv + Vercel/Netlify**.
> الملف جاهز للتسليم لـ Claude Code كـ spec للبناء.

---

## 1. الهدف والمبادئ

- منصة مقالات عربية بسيطة، **RTL أولاً**، سريعة، وثابتة (static).
- محتوى المقالات مصدره ملفات Markdown داخل المستودع (لا CMS خارجي في البداية).
- النشرة البريدية عبر **beehiiv** (نموذج اشتراك مضمّن + إرسال من لوحة beehiiv).
- النشر تلقائي: `git push` ← بناء ← نشر على CDN.

---

## 1.5 الهوية البصرية (الاتجاه المختار: الشبكة النظيفة)

الطابع العام: عصري، هادئ، مساحات بيضاء واسعة، شبكة بطاقات مرتّبة، ولون واحد بارد. بسيط ونظيف — لا رسوم توضيحية كثيفة.

**اسم الموقع:** «مِداد» (مؤقت — قابل للتغيير لاحقاً).
عرّفه كمتغيّر واحد في `src/config.ts` واقرأه في كل الصفحات، عشان تغيير الاسم لاحقاً يتم من مكان واحد:

```ts
export const SITE_NAME = "مِداد";
export const SITE_TAGLINE = "مساحة عربية للقراءة الهادئة";
export const SITE_DESCRIPTION = "مقالات مختارة في الفكر والثقافة والتقنية.";
```

**الخط:** IBM Plex Sans Arabic (مجاني من Google Fonts). الأوزان: 400 للنص، 500 للعناوين الفرعية والوسوم، 700 للوردمارك.

**لوحة الألوان:**

| الدور | القيمة |
|---|---|
| الخلفية | أبيض / رمادي فاتح جداً |
| النص الأساسي | شبه أسود |
| النص الثانوي | رمادي متوسط |
| الحدود | رمادي فاتح (0.5px) |
| اللون المميّز (accent) | أخضر بارد `#0F6E56` — يُستخدم بحذر: الوسوم، زر الاشتراك، روابط بسيطة |

**قواعد التصميم:**
- بطاقات المقالات: حد 0.5px، زوايا دائرية 12px، صورة أعلى + وسم + عنوان + اسم الكاتب.
- شبكة مرنة: `repeat(auto-fit, minmax(165px, 1fr))` بمسافة 14px.
- مساحات بيضاء سخية بين الأقسام.
- الشعار مبدئياً **وردمارك نصّي** (الاسم بالخط أعلاه) — يُستبدل بملف شعار لاحقاً.

---

## 2. المكدّس التقني

| الطبقة | الأداة |
|---|---|
| إطار العمل | Astro (Content Collections) |
| المحتوى | Markdown / MDX + front-matter |
| التنسيق | CSS (RTL) — أو Tailwind مع `dir="rtl"` |
| الخط | خط عربي (مثل: IBM Plex Arabic / Tajawal / Noto Kufi) |
| النشرة | beehiiv (embed form + API عند الحاجة) |
| الاستضافة | Vercel أو Netlify |
| المستودع | GitHub |

---

## 3. خريطة الصفحات (Routes)

| المسار | الوصف |
|---|---|
| `/` | الرئيسية: hero + أحدث المقالات (شبكة) + معرض الأعمال + نبذة + نموذج النشرة |
| `/blog` | قائمة كل المقالات + **ترقيم صفحات** (بطاقة: صورة + عنوان + كاتب + تاريخ) |
| `/blog/page/[n]` | صفحات الترقيم |
| `/post/[slug]` | صفحة المقال الواحد |
| `/blog/tags/[tag]` | صفحة تصفية حسب الوسم |
| `/author/[name]` | صفحة الكاتب (كل مقالاته) |
| `/projects` | كُتب وأفلام (معرض الأعمال) |
| `/projects/[slug]` | صفحة عمل واحد (كتاب / فيلم) |
| `/about` | عنّا |
| `/contact` | راسلنا |
| `/society` | سياسة وشروط النشر (صفحة ثابتة) |

---

## 4. نموذج بيانات المقال (front-matter)

> **مهم:** القيم أدناه **أمثلة توضّح الشكل فقط**، وهي **متغيّرة لكل مقال**. كل مقال ملف مستقل بقيمه الخاصة (العنوان، الكاتب، التاريخ، الغلاف، الوسوم…). القوالب (templates) تقرأ هذي القيم **ديناميكياً** لكل مقال عبر `entry.data.*`، فلا تُكتب أي قيمة يدوياً في القالب.

كل مقال ملف `.md` تحت `src/content/blog/` بهذا الشكل:

```yaml
---
title: "عنوان المقال"
slug: "anatomy-of-memory"
author: "اسم الكاتب"
date: 2026-06-18
cover: "../../assets/covers/anatomy.jpg"
description: "وصف مختصر للـ SEO والبطاقة"
readingTime: 6           # بالدقائق (أو يُحسب تلقائياً)
tags: ["نقد", "فن", "ذاكرة"]
sources:                 # اختياري — تظهر أسفل المقال
  - "اسم المرجع - المؤلف - المترجم"
draft: false
---

نص المقال بصيغة Markdown، يدعم العناوين الفرعية والصور والاقتباسات.
```

نموذج بيانات العمل (`src/content/projects/`): `title, slug, type (كتاب/فيلم), cover, description, year, link`.

---

## 4.1 الثابت مقابل المتغيّر (مرجع سريع)

| ثابت في كل الموقع (يُكتب مرة) | متغيّر لكل مقال (يقرأه القالب ديناميكياً) |
|---|---|
| الشعار والاسم | العنوان `title` |
| قائمة التنقّل | الكاتب `author` |
| روابط التواصل | تاريخ النشر `date` |
| نموذج النشرة (beehiiv) | صورة الغلاف `cover` |
| التذييل وحقوق النشر | الوصف `description` |
| الخط والألوان والتنسيق | وقت القراءة `readingTime` |
| نصوص الصفحات الثابتة (عنّا، راسلنا، سياسة النشر) | الوسوم `tags` والمصادر `sources` والنص |

الخلاصة: القالب يُبنى **مرة واحدة**، وكل مقال يعبّي خاناته بنفسه. إضافة مقال جديد = ملف Markdown جديد فقط، بدون لمس القالب.

---

## 5. المكوّنات المشتركة (Components)

- **Header**: شعار + قائمة تنقّل (مقالات، كُتب وأفلام، عنّا، راسلنا) + قائمة موبايل.
- **Footer** (في كل صفحة): شعار + روابط تواصل (Instagram, YouTube, X, LinkedIn) + **نموذج النشرة (beehiiv)** + حقوق النشر.
- **ArticleCard**: صورة + عنوان + كاتب + تاريخ (مستخدمة في الرئيسية و`/blog`).
- **NewsletterForm**: نموذج beehiiv (حقل البريد + زر «سجّل» + رسالة «شكرًا على تسجيلك»).
- **Pagination**: أزرار الصفحات في `/blog`.
- **RelatedPosts**: 3 مقالات ذات صلة أسفل صفحة المقال.
- **TagList**: وسوم قابلة للنقر تؤدي لصفحة الوسم.

---

## 6. صفحة المقال — العناصر بالترتيب

1. العنوان (H1)
2. سطر الكاتب + التاريخ + وقت القراءة
3. صورة الغلاف
4. نص المقال (عناوين فرعية، صور بينية، اقتباسات)
5. المصادر (إن وُجدت)
6. الوسوم (Tags)
7. مقالات ذات صلة (Related)
8. التذييل مع نموذج النشرة

---

## 7. ربط النشرة (beehiiv)

- أنشئ منشوراً (publication) في beehiiv واحصل على **كود الـ embed** لنموذج الاشتراك.
- ضع النموذج داخل مكوّن `NewsletterForm` وثبّته في التذييل.
- (اختياري لاحقاً) استخدم **beehiiv API** إذا رغبت بنموذج مخصص بدل الـ embed.
- الإرسال الفعلي للنشرة يتم من لوحة beehiiv، مو من الموقع.

---

## 8. RTL و i18n

- `<html lang="ar" dir="rtl">`.
- تأكد من انعكاس: الهوامش/الحشو، اتجاه الأيقونات، محاذاة النصوص.
- أرقام عربية (1،2،3) وعلامات ترقيم عربية «...» حسب أسلوب المحتوى.
- خط عربي واضح للعناوين والنص.

---

## 9. SEO والوصول

- لكل صفحة: `title`, `meta description`, **Open Graph** + **Twitter Card** (الموقع المرجعي يهتم بها).
- `canonical` لكل مقال.
- خريطة موقع (`sitemap`) و`robots.txt` — عبر إضافة Astro الرسمية.
- بيانات مقال منظمة (`Article` schema) اختياري.

---

## 10. النشر (Deploy)

- اربط مستودع GitHub بـ Vercel/Netlify.
- كل `push` على الفرع الرئيسي = بناء ونشر تلقائي.
- أضف الدومين + شهادة HTTPS تلقائية.

---

## 11. ترتيب البناء المقترح لـ Claude Code

1. تهيئة مشروع Astro + إعداد RTL + الخط.
2. Layout عام (Header + Footer + نموذج النشرة placeholder).
3. Content Collections للمقالات + الأعمال (schema حسب القسم 4).
4. صفحة `/blog` + ArticleCard + الترقيم.
5. صفحة `/post/[slug]` بكل عناصر القسم 6.
6. صفحات الوسوم والكاتب.
7. `/projects` + `/projects/[slug]`.
8. الصفحات الثابتة: `/about`, `/contact`, `/society`.
9. الرئيسية `/` (تجميع المكوّنات).
10. ربط beehiiv الفعلي + SEO + الدومين + النشر.

---

## ملاحظة عن المحتوى المجتمعي

الموقع المرجعي يعتمد على **مساهمات كُتّاب**: يُرسل الكاتب المقال بالبريد، يراجعه فريق التحرير، ثم يُنقّح ويُنشر. في نسختك الثابتة، هذا يترجم إلى:
**استلام بالبريد ← مراجعة ← تحويل النص إلى ملف Markdown مع front-matter ← `git push` ← نشر تلقائي.**
لاحقاً يمكن أتمتة هذا عبر لوحة CMS (مثل Decap/Keystatic) إذا كبر المحتوى.

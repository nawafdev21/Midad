# تعليمات المشروع (CLAUDE.md)

هذا موقع مقالات ونشرة بريدية عربية باسم «مِداد». اقرأ `SPEC.md` واعتبره **المصدر الوحيد للحقيقة** لكل قرارات البناء.

## المكدّس
- Astro (SSG) + Content Collections للمقالات (Markdown).
- خط IBM Plex Sans Arabic من Google Fonts.
- النشرة عبر beehiiv (نموذج embed لاحقاً).
- النشر على Vercel أو Netlify.

## قواعد ثابتة (التزم بها دائماً)
- **RTL أولاً:** `<html lang="ar" dir="rtl">` وكل التخطيط معكوس بشكل صحيح.
- **اسم الموقع متغيّر واحد:** عرّف `SITE_NAME` في `src/config.ts` واقرأه في كل مكان. لا تكتب «مِداد» يدوياً في أي صفحة.
- **الهوية البصرية:** الاتجاه «الشبكة النظيفة» — بطاقات مرتّبة، مساحات بيضاء واسعة، لون مميّز واحد بارد (`#0F6E56`). بسيط ونظيف بلا زخرفة.
- **قيم المقال متغيّرة لكل مقال** وتُقرأ ديناميكياً عبر `entry.data.*`. لا تضع قيماً ثابتة في القوالب.

## أسلوب العمل
- **خطوة واحدة فقط في كل مرة** حسب ترتيب البناء في القسم ١١ من `SPEC.md`. لا تنتقل للخطوة التالية إلا بعد موافقتي.
- قبل أي خطوة، اذكر باختصار وش بتسوي فيها.
- **كود بسيط ونظيف**، بأقل اعتماديات ممكنة. لا تضف مكتبات ثقيلة إلا لضرورة، واشرح السبب.
- تعليقات قليلة وواضحة.
- بعد كل خطوة، قل لي كيف أعاين النتيجة محلياً (`npm run dev`).

## تواصل
- خاطبني بالعربية. المصطلحات التقنية بالإنجليزية عادي.
- ردود مختصرة ومباشرة.
